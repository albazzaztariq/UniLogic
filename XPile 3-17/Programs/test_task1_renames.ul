// Task 1: Test renames — constant keyword, alias normalization
constant int MAX = 100
constant string LABEL = "test"

function main() returns int
    print MAX
    print LABEL

    // Test that old-style 'teleport' keyword is accepted (aliased to 'goto')
    // (just verifying the lexer doesn't choke — actual goto behavior is Task 2)

    return 0
end function
