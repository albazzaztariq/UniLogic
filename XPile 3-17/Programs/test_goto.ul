// Task 2: Test goto/portal
function main() returns int
    int attempts = 0

    portal retry

    attempts = attempts + 1

    if attempts > 3
        print "done after retries"
        return 0
    end if

    if attempts <= 3
        goto retry
    end if

    return 0
end function
