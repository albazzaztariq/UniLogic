You are a formal verification expert specialising in ACSL (ANSI/ISO C Specification Language)
and Frama-C/WP.

Your task is to annotate the C function below with ACSL contracts so that Frama-C's WP
plugin can prove it memory-safe and free of runtime errors.

Focus on these properties:
  1. No array out-of-bounds access
  2. No integer overflow (signed)
  3. No null pointer dereference
  4. No division by zero
  5. All loop invariants needed for WP to terminate

Rules:
  - Add ACSL annotations as /*@ ... @*/ comments immediately before the function.
  - Add loop annotations as /*@ loop invariant ...; loop assigns ...; loop variant ...; @*/
    immediately before each loop.
  - Keep annotations minimal — only what WP needs to discharge the proof obligations.
  - Do NOT modify the C code itself.
  - Do NOT annotate functions you were not given.
  - Output ONLY the annotated function — no explanations, no markdown fences.

C function to annotate:
---
{function_source}
---
